<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.easyhooon.metroapplication.MetroApplication"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="com.easyhooon.metroapplication.di.MetroAppComponentFactory"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetroApplication">

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="com.easyhooon.metroapplication.initializer.FirebaseCrashlyticsInitializer"
                android:value="androidx.startup" />

            <meta-data
                android:name="com.easyhooon.metroapplication.initializer.ComposeStabilityAnalyzerInitializer"
                android:value="androidx.startup" />

        </provider>

        <service
            android:name=".MetroFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
